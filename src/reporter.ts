// Generates a human-readable report from attack results

import type { AttackResult } from "./log";

export async function generateReport(_results: AttackResult[]): Promise<string> {
  return "report not implemented yet";
}
